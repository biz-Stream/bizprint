# CheckStyle コーディング規約

Java ソースを変更する際は以下のルールに従うこと:

1. 未使用 import の削除
2. import 順序の整理（java → javax → org → com.brainsellers）
3. スター import の排除（static import は可）
4. インデントの統一（スペース4）
5. 中括弧スタイルの統一（K&R: 開き括弧は行末）
6. フィールド・変数名は camelCase、定数は UPPER_CASE
7. 演算子の前後にスペースを追加
8. タブ文字は使用せずスペース4に統一
